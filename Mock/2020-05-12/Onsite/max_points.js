/**
 * @param {number[][]} points
 * @return {number}
 */
var maxPoints = function(points) {
    let max = 0;

    while( points.length != 0 ) {
        for( let i = 0; i < points.length; i++ ) {
            
        }
    }


};